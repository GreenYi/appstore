networks:
    1panel-network:
        external: true
services:
  danted:
    image: p3terx/ariang:latest
    container_name: ${CONTAINER_NAME}
    restart: always
    networks:
      - 1panel-network
    ports:
      - "${PANEL_APP_PORT_HTTP}:6880"
    labels:
      createdBy: "Apps"
